---
import Layout from './default.astro'

type Props = {
  title: string
}

const { title } = Astro.props
---

<Layout title={title}>
  <article>
    <slot />
  </article>
</Layout>

<style>
  article {
    max-width: 50rem;
    margin: 0 auto;
    padding-inline: var(--space3);
  }
</style>
